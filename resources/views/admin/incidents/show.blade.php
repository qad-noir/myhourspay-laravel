@extends('layouts.admin') @section('title','Incident '.$incident->reference) @section('content')
<a class="admin-context-back" href="{{ route('admin.incidents.index') }}"><svg viewBox="0 0 20 20"><path d="m12.5 5-5 5 5 5"/></svg>Back to incidents</a><section class="admin-card admin-log-detail"><dl><dt>Event</dt><dd>{{ $incident->event_type }}</dd><dt>Status</dt><dd>{{ $incident->resolved_at?'Resolved':'Open' }}</dd><dt>Name / email</dt><dd>{{ $incident->submitted_name }} · {{ $incident->submitted_email }}</dd><dt>Exception</dt><dd>{{ $incident->exception_class }}</dd><dt>Safe message</dt><dd>{{ $incident->exception_message }}</dd><dt>Occurred</dt><dd>{{ $incident->occurred_at }}</dd></dl>@if($incident->resolved_at)<div class="incident-resolution"><strong>Resolved by {{ $incident->resolver?->name??'Deleted administrator' }}</strong><p>{{ $incident->resolution_notes }}</p><form method="POST" action="{{ route('admin.incidents.reopen',$incident) }}">@csrf<button class="admin-primary-action">Reopen incident</button></form></div>@else<form class="incident-resolution" method="POST" action="{{ route('admin.incidents.resolve',$incident) }}">@csrf<label>Resolution notes<textarea name="resolution_notes" required></textarea></label><button class="admin-primary-action">Mark resolved</button></form>@endif</section>
@endsection
