<div class="admin-row-actions">@if($user->trashed())<form method="POST" action="{{ route('admin.users.restore',$user->id) }}">@csrf<button>Restore</button></form><form method="POST" action="{{ route('admin.users.force-delete',$user->id) }}" data-confirm="Permanently delete this user and dependent data?">@csrf @method('DELETE')<button class="is-danger">Delete forever</button></form>@else<a href="{{ route('admin.users.show',$user) }}">View / edit</a><form method="POST" action="{{ route('admin.users.suspension',$user) }}">@csrf<button>{{ $user->suspended_at?'Reactivate':'Suspend' }}</button></form><form method="POST" action="{{ route('admin.users.verification',$user) }}">@csrf<button>{{ $user->email_verified_at?'Revoke verification':'Verify' }}</button></form>@unless($user->email_verified_at)<form method="POST" action="{{ route('admin.users.verification.resend',$user) }}">@csrf<button>Resend code</button></form>@endunless<form method="POST" action="{{ route('admin.users.workspace-reset',$user) }}" data-confirm="Require this user to create a new workspace?">@csrf<button>Reset workspace</button></form><form method="POST" action="{{ route('admin.users.destroy',$user) }}" data-confirm="Move this user and owned data to trash?">@csrf @method('DELETE')<button class="is-danger">Trash</button></form>@endif</div>
