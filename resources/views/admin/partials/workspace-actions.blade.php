<div class="admin-row-actions">@if($workspace->trashed())<form method="POST" action="{{ route('admin.workspaces.restore',$workspace->id) }}">@csrf<button>Restore</button></form><form method="POST" action="{{ route('admin.workspaces.force-delete',$workspace->id) }}" data-confirm="Permanently delete this workspace and its hours?">@csrf @method('DELETE')<button class="is-danger">Delete forever</button></form>@else<a href="{{ route('admin.workspaces.show',$workspace) }}">View / edit</a><form method="POST" action="{{ route('admin.workspaces.destroy',$workspace) }}" data-confirm="Move this workspace and its hours to trash?">@csrf @method('DELETE')<button class="is-danger">Trash</button></form>@endif</div>
