<div class="admin-row-actions">@if($entry->trashed())<form method="POST" action="{{ route('admin.hours.restore',$entry->id) }}">@csrf<button>Restore</button></form><form method="POST" action="{{ route('admin.hours.force-delete',$entry->id) }}" data-confirm="Permanently delete this hours entry?">@csrf @method('DELETE')<button class="is-danger">Delete forever</button></form>@else<a href="{{ route('admin.hours.edit',$entry) }}">Edit</a><form method="POST" action="{{ route('admin.hours.destroy',$entry) }}" data-confirm="Move this hours entry to trash?">@csrf @method('DELETE')<button class="is-danger">Trash</button></form>@endif</div>
